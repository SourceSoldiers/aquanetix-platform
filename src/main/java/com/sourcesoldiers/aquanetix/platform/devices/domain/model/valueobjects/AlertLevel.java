package com.sourcesoldiers.aquanetix.platform.devices.domain.model.valueobjects;

/**
 * Severity assigned to a threshold configuration when its bounds are
 * exceeded.
 *
 * @since 1.0
 */
public enum AlertLevel {
    WARNING,
    CRITICAL
}
